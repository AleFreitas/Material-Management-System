export type Livro = {
    isbn: string,
    descricao: string,
    data_aquisicao: string,
    conservacao: string,
    localizacao: string,
    quantidade: number,
    titulo: string,
    url_capa: string,
};
